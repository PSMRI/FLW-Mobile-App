<?xml version="1.0" encoding="utf-8"?>
<!-- Quiet lilac "New" chip. The CTA pill is the card's one bold accent, so this
     stays a tint rather than competing with it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/recap_playback_progress_track" />
    <corners android:radius="12dp" />
</shape>
